<% if current_user.following?(user) %>
	<%= link_to ' 解除', user_relationships_path(user.id), method: :delete, class: 'fas fa-user-minus' %>
<% else %>
	<%= link_to ' フォロー', user_relationships_path(user.id), method: :POST, class: 'fas fa-user-plus' %>
<% end %>